# Phase 10 Execution Plan

Date: 2026-08-25
Branch: `product/product-truth-daily-workflow`

1. Preserve the Phase 9 baseline and user-owned untracked files; record exact source gates before edits.
2. Add typed, project-isolated read-only aggregation services for Home, Project Overview, Activity/Episode Detail, Regression Detail and Diagnostics. Reuse migration `0009`; add no migration unless implementation proves a persistent field is essential.
3. Rebuild the desktop information hierarchy around Home, Projects, Alerts and Settings. Make Project Overview the daily project center and keep Diagnostics contextual.
4. Make First Run a real accessible selection and settings flow whose completion opens the selected destination.
5. Present Episodes, checks, behaviors, regressions, repair candidates, Apply and rollback as evidence-bound operational components with progressive technical disclosure.
6. Complete disconnected-project, notification-route, native-tray, activity-mode, support-bundle, updater, Diagnostics and Product Self-Test surfaces without acceptance-only hero placeholders.
7. Add English base keys and exact Hebrew parity/RTL; keep every visible string, accessible label, placeholder and error in the catalogs.
8. Add project-isolation, privacy, route-allowlist, no-duplicate-refresh, accessibility and state-truth tests. Use only Demo Lab and disposable fixtures.
9. Capture all 36 required states at deterministic synthetic boundaries, generate the manifest, screen guide and PDF, and label the web tray as test-only preview.
10. Run complete source gates, build fresh Intel macOS artifacts, run Phase 8/9/10 packaged validators, install only after mandatory gates pass, then scan and create one local commit and—only on full pass—the local annotated Phase 10 tag.
11. Keep macOS and Windows parallelizable from the same repository, source, API,
    migration and version. Provide pinned bootstrap/build/validation wrappers and a
    Windows x64 NSIS acceptance workflow, but keep Windows status honest until native
    runtime lifecycle evidence exists.

## Explicit non-goals

No AI/model/provider integration, prompt access, analytics, cloud or source upload, automatic repair, automatic Apply, real-project acceptance, APC change, push, public release, signing, notarization, platform-specific product fork, or unverified-platform certification.
